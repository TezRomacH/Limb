using UnityEngine;
using System.Collections;

public class StreetLamp : MonoBehaviour {

    /*private const ushort range = 12;

    void Start()
    {
        GetComponent<Light>().enabled = false;
        InvokeRepeating("CheckPlayerAndEnableLamp", 1, 1);
    }

    void CheckPlayerAndEnableLamp()
    {
        if (Vector3.Distance(transform.position, Managers.player.transform.position) <= range)
        {
            GetComponent<Light>().enabled = true;
            /*
             Звук включения лампы
             
        }
    }*/
}
